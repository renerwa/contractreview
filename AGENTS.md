# 协作规则（AI / 开发）

本仓库协作（含我与您之间的沟通、代码编写与代码评审）遵循以下规则：

## 1. 始终用中文沟通

- 我与您沟通时，**始终使用中文**（包括：需求澄清、方案讨论、排查问题、代码评审意见、提交说明建议等）。
- 代码中的标识符（变量名/函数名/文件名）是否使用英文，遵循项目既有风格；本规则不强制将标识符改为中文。

## 2. 编写代码时，核心逻辑添加中文注释

- 当我新增或修改代码时，**涉及核心逻辑的部分必须添加中文注释**。
- “核心逻辑”通常包括但不限于：
  - 关键业务规则与判断分支（为什么这样判断、边界条件是什么）
  - 解析/抽取/校验/容错与降级策略（失败模式与回退行为）
  - 关键流程编排/状态机（步骤含义与前后约束）
  - 非直观的算法或性能关键路径（复杂度、缓存、并发与资源约束）
- 注释侧重说明“意图/原因/边界”，避免逐行翻译式注释。

## 3. 所有页面按多语言设计

- 当我新增或修改页面时，**必须按多语言方式设计与实现**，不能将面向用户的文案直接写死在页面组件中。
- 页面涉及的文案、标签、按钮文本、提示语等，统一维护在 `src/config/locale` 目录下的多语言文件中。
- 即使当前仅上线单一语言，也要预留好多语言扩展位，确保页面结构、文案读取方式与后续语言切换兼容。

## 4. 修改页面代码时关注 SEO

- 当我新增或修改页面时，**必须关注 SEO 影响**，因为本网站主要依赖搜索引擎自然流量获取用户。
- 页面结构应优先保证搜索引擎可理解、可抓取：合理使用 `title`、`description`、语义化标签、清晰的 `h1`/`h2` 层级、可索引的正文内容与内部链接。
- 涉及图片、图标或媒体资源时，应补充合适的 `alt`、尺寸与加载策略，避免影响页面可访问性、内容理解和核心性能指标。
- 修改页面交互或渲染方式时，应避免把关键 SEO 内容变成搜索引擎难以抓取的纯客户端动态内容；如必须动态渲染，应确认已有服务端渲染、静态生成或元数据兜底方案。
- 新增或调整面向搜索流量的页面时，应同步考虑 canonical、Open Graph、结构化数据、URL 稳定性、页面加载性能与多语言 SEO 兼容性，避免造成收录、排名或分享预览退化。

## 5. 项目定义

这是一个面向企业合同场景的 AI 合同审查项目，核心目标是帮助法务、采购、销售和商务团队更高效地识别合同风险、理解条款问题，并获得结构化的修改建议。项目主流程包括合同上传或粘贴、合同解析、预分析、正式审查、结果查看与导出。系统会结合合同类型、审查立场、签约地和用户关注点，生成带有风险等级、条款依据、问题说明和修改建议的结构化审查结果。

产品调性上，这个项目强调“科技感 + 专业可信感”：既要体现 AI 驱动的智能审查能力，也要让用户感受到审查过程严肃、结果可靠、适合正式业务场景使用。它不是通用 AI 聊天网站，也不是传统内容站，而是一个围绕合同审查工作流构建的垂直 AI SaaS 产品。

- 当你理解这个项目时，应优先把它视为“企业级 AI 合同审查平台”，而不是通用 AI 工具站。
